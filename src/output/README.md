place images acting as "current version" here.

whiteSocket output images will be added to this directory.

note: output images in DEV mode (non-executable) will be in auto-created `output/` at the top directory